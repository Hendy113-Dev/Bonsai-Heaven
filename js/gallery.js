document.addEventListener("DOMContentLoaded",async()=>{const grid=document.querySelector("#gallery-grid");if(!grid)return;try{const r=await fetch("gallery.json");if(!r.ok)throw new Error("Gallery data not found");const items=await r.json();if(!items.length){grid.innerHTML='<div class="gallery-empty">Gallery photographs coming soon.</div>';return;}grid.innerHTML=items.map(x=>`<figure class="gallery-item"><img loading="lazy" src="${x.image}" alt="${escapeGallery(x.title||'Horizon Bonsai gallery image')}"><figcaption class="gallery-caption"><strong>${escapeGallery(x.title||'Horizon Bonsai')}</strong>${x.caption?`<span>${escapeGallery(x.caption)}</span>`:''}</figcaption></figure>`).join('');}catch(e){grid.innerHTML='<div class="gallery-empty">Gallery photographs coming soon.</div>';}});function escapeGallery(s){return String(s??'').replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]));}